# 02: Панель фильтров в DetailPage (вариант A)

**What to build:** в шапке CONSOLE на Detail page — тулбар фильтров из прототипа A: поле текстового поиска, время-комбо (Всё/5/10/30 мин), кнопка сброса, счётчик «Показано X из Y», лента чипсов level/channel из наблюдаемых значений. Несовпавшие строки скрыты; пустое состояние объясняет причину (включая число скрытых нераспознанных); жирные строки обрезаны на ~500 символов с «развернуть». По умолчанию всё выключено (лог как сейчас), выбор живёт пока открыта Detail page.

**Blocked by:** 01 (Парсер логов).

**Status:** done (2026-09-16: фильтры в LogModel + панель A в DetailPage + test_logmodel 10/10; trun-app/qmltests зелёные 30/30; визуальная проверка панели — вручную в запущенном приложении)

- [ ] AND-логика четырёх фильтров; пустой фильтр = выключен
- [ ] Чипсы строятся из потока, словари не захардкожены; фильтр «канал» = target слоя 1 ∪ channel слоя 2
- [ ] Живой поток фильтруется на лету, автоскролл как сейчас; паузы нет
- [ ] Ресурсы только через `qrc:/`; палитра/радиусы из Theme; макет сверен с `../console-filters/prototype/FilterBarPrototype.qml` (вариант A)
