Type: prototype
Status: resolved
Blocked by: 03

## Question

Как выглядит панель фильтров в шапке CONSOLE (рамка из скриншота): грубый кликабельный макет, по которому человек скажет «да, так» или «нет, не так»?

Сделать через skill `prototype`: QML-набросок в DetailPage-стилистике (Theme, StatCard-ряд выше, CONSOLE-карточка ниже) — поле поиска, чипсы level/channel, селектор времени, кнопка сброса, счётчик. Интерактивность минимальная (фильтрует статичный массив строк-примеров из корпуса). Артефакт — ссылкой из тикета, не пастой кода в тело. Решений о парсинге здесь не принимать.

## Asset

`../prototype/FilterBarPrototype.qml` (+ `VariantA/B/C.qml`, `LogRow.qml`, `LogParse.js`, `SampleData.js`). Запуск: `qml FilterBarPrototype.qml` из каталога прототипа; переключение вариантов — плавающая пилюля снизу или ←/→. Проверено: qmllint без ошибок, offscreen-прогон без ошибок движка.

## Answer

Вердикт человека (2026-09-16): забираем вариант **A «Тулбар»** — один ряд (поиск + время-комбо + сброс + счётчик) + плоская лента чипсов. B/C отброшены без заимствований. При fold-in в DetailPage: палитру и радиусы взять из Theme (в прототипе захардкожены), движок фильтров переписать на C++/модель (JS из LogParse.js — только референс логики AND/hide/счётчика/обрезки 500), мок-данные выкинуть, вариант-переключатель и B/C не переносить.
