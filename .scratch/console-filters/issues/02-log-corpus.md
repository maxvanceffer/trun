Type: task
Status: resolved
Blocked by:

## Question

Собрать корпус реальных строк Console history trun (10–50 строк каждого типа): monolog/serve из скриншота, npm/vite, docker, прочие раннеры — обезличенно (без секретов), одним артефактом, чтобы парсинг-решения проверялись на фактах, а не на одной строке из постановки.

Что сделать: снять `read_log` / Console history нескольких живых Run, сохранить сырые строки (с ANSI как есть + помеченные), приложить файл-артефакт ссылкой из тикета. Никаких решений о форматах здесь не принимать — только сырьё для тикетов про парсинг и семантику. HITL, если доступ к проектам есть только у человека: отдать ему чеклист команд.

## Answer

Готово AFK по файловому логу + транскрипции консоли (живой Run снимать не понадобилось — человек дал путь к dev.log). Артефакт: `../corpus-sample.log` (95 строк).

Состав: источник A — dev.log (225k строк, скан первых 120k): по 4 строки на каждый из 8 каналов (request, php, doctrine, security, app, messenger, console, cache), все редкие уровни выборкой (CRITICAL 22, WARNING 26, NOTICE 5 + 5 ERROR), самый жирный doctrine.DEBUG целиком (28k символов — для решения об обрезке). Источник B — 7 строк консольного serve-вывода (ветка-кастом `[channel] Mon DD |LEVEL|`, каналы Application/Web Server, субканалы DOCTRI/SECURI/SERVER) из постановки и скриншота, длинные SQL помечены `[...]`.

Факты из сбора: файловые логи строго однострочные (0 нематчащихся строк из 60k — стектрейсы внутри строки как `\n`-эскейпы); DEBUG ~82–87% потока; пароли в источнике уже `<redacted>`, email/wdt-токены зачищены при семплировании, проверка на секреты — чисто. npm/vite/docker-раннеров в этом корпусе нет — доберутся при grilling-семантике, если понадобятся.

## Comments
