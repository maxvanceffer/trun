# Test data file for Qt's QFINDTESTDATA function
# This file should be placed in the same directory as test assets
# so that tests can reference files in the build tree (e.g. qrc:/qml/...)

set(TEST_DATA_DIR "${CMAKE_CURRENT_SOURCE_DIR}/tests")
